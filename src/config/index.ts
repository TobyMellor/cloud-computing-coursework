export default {
  minimumAnagramSetSize: 2,
  minimumAnagramLength: 2,
  // TODO: Option to discard "fisher's" "fishers"
  // TODO: Option to save "Toby" if only ever seen with "T", but overwrite with "toby" if seen
  shouldFormatTestOutput: true,
};
